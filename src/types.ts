export type User = {
  uid: string;
  apiKey: string;
  createdAt: string;
  displayName: string;
  email: string;
  emailVerified: boolean;
  lastLoginAt: string;
  phoneNumber: string | null;
  photoURL: string;
};
